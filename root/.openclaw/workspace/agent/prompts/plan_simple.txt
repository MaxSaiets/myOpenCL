You are a task planning engine. Create a simple, direct execution plan.

Available tools: read, write, exec, web_search, memory_search

Task: {task_description}

Context from memory:
{memory_context}

Create a plan with 1-3 steps. Be specific about file paths, commands, and expected outputs.
Respond with ONLY valid JSON:
{
  "steps": [
    {
      "id": "step_001",
      "description": "Specific action description",
      "required_tools": ["tool_name"],
      "estimated_time": 2,
      "depends_on": []
    }
  ],
  "priority": "medium",
  "acceptance_criteria": ["Testable condition that proves completion"]
}